set dotenv-load := true

# Lint everything
lint:
  carton run perlcritic --harsh ./Stardeck/lib/*.pm ./bin/*  # TODO: Pass --cruel

# Format everything
format:
  carton run perltidy -b -bext='/' ./Stardeck/lib/*.pm ./bin/*

# Run tests
test:
  cd Stardeck && perl Makefile.PL && make test
