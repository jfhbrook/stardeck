- name: Pipewire
  hosts: localhost
  connection: local
  tasks:
    - name: Create /etc/pipewire
      become: true
      ansible.builtin.file:
        path: /etc/pipewire
        state: directory
        owner: root
        group: root
        mode: "0755"
